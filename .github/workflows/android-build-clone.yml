name: Android Build - Fixed Clone

on:
  push:
    branches:
      - main
    paths:
      - 'app/**'
      - 'gradle/**'
      - 'build.gradle*'
      - 'settings.gradle*'
      - 'gradle.properties'
      - 'web-chat/**'
      - 'tools/**'
      - '.github/workflows/android-build-clone.yml'
  workflow_dispatch:
    inputs:
      gradle_task:
        description: Gradle task
        default: ':app:assembleCloneRelease'
        type: choice
        options:
          - ':app:assembleCloneDebug'
          - ':app:assembleCloneRelease'
          - ':app:assembleDebug'
          - ':app:assembleNightly'

concurrency:
  group: android-clone-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  JAVA_VERSION: '21'
  NODE_VERSION: '22'
  ANDROID_NDK_VERSION: '27.0.12077973'
  ANDROID_CMAKE_VERSION: '3.22.1'
  MANUAL_DEPS_DIR: manual-deps

jobs:
  build:
    name: Build Clone APK (Fixed)
    runs-on: ubuntu-24.04
    timeout-minutes: 240

    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 1
          submodules: recursive

      - name: Free disk space
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: false
          android: false
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: false

      - name: Set up swap 10GB
        uses: pierotofy/set-swap-space@master
        with:
          swap-size-gb: 10

      - name: Fix Gradle mirror + wrapper
        run: |
          cat gradle/wrapper/gradle-wrapper.properties
          sed -i 's|https://mirrors.aliyun.com.*|https://services.gradle.org/distributions/gradle-8.13-bin.zip|g' gradle/wrapper/gradle-wrapper.properties || true
          if grep -q "aliyun" gradle/wrapper/gradle-wrapper.properties; then
            printf "distributionBase=GRADLE_USER_HOME\ndistributionPath=wrapper/dists\ndistributionUrl=https\\://services.gradle.org/distributions/gradle-8.13-bin.zip\nzipStoreBase=GRADLE_USER_HOME\nzipStorePath=wrapper/dists\n" > gradle/wrapper/gradle-wrapper.properties
          fi
          cat gradle/wrapper/gradle-wrapper.properties

      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}

      - uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}

      - uses: android-actions/setup-android@v4

      - name: Cache Gradle
        uses: actions/cache@v5
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-clone-ndk27-v2-${{ hashFiles('gradle/wrapper/gradle-wrapper.properties') }}
          restore-keys: |
            gradle-clone-ndk27-v2-

      - name: Install SDK/NDK/CMake
        run: |
          sdkmanager --install "platform-tools" "platforms;android-34" "platforms;android-36" "build-tools;34.0.0" "build-tools;35.0.0" "ndk;$ANDROID_NDK_VERSION" "cmake;$ANDROID_CMAKE_VERSION"

      - name: Restore manual deps cache
        id: cache-manual-deps
        uses: actions/cache@v5
        with:
          path: ${{ env.MANUAL_DEPS_DIR }}
          key: manual-deps-full-${{ hashFiles('ci/script/download_android_dependencies.sh') }}

      - name: Download Android dependencies
        if: steps.cache-manual-deps.outputs.cache-hit != 'true'
        run: bash ci/script/download_android_dependencies.sh full "$MANUAL_DEPS_DIR"

      - name: Prepare Android dependencies
        run: |
          python3 ci/script/prepare_android_dependencies.py --profile full --archives "$MANUAL_DEPS_DIR" --repository "$GITHUB_WORKSPACE"

      - name: Cache Rust cargo
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            tools/native_ripgrep/target
          key: ripgrep-${{ hashFiles('tools/native_ripgrep/Cargo.lock') }}

      - name: Build native ripgrep
        run: |
          set -euo pipefail
          export ANDROID_NDK_HOME="$ANDROID_HOME/ndk/$ANDROID_NDK_VERSION"
          export CARGO_TARGET_AARCH64_LINUX_ANDROID_LINKER="$ANDROID_NDK_HOME/toolchains/llvm/prebuilt/linux-x86_64/bin/aarch64-linux-android26-clang"
          rustup toolchain install 1.88.0 --profile minimal
          rustup target add --toolchain 1.88.0 aarch64-linux-android
          cargo +1.88.0 build --manifest-path tools/native_ripgrep/Cargo.toml --release --target aarch64-linux-android --locked
          install -Dm755 tools/native_ripgrep/target/aarch64-linux-android/release/liboperit_ripgrep.so app/src/main/jniLibs/arm64-v8a/liboperit_ripgrep.so

      - name: Clean corrupted CXX cache (fix sherpa_ncnn / wamr)
        run: |
          echo "Cleaning CXX caches that cause 'does not contain CMakeLists.txt'"
          rm -rf app/.cxx
          rm -rf app/src/main/cpp/.cxx
          rm -rf app/build/intermediates/cxx
          rm -rf app/build/.cxx
          rm -rf llm/mnn/.cxx
          rm -rf mnn/.cxx
          ls -la app/src/main/cpp/ | head -20

      # >>> FIX PENTRU EROAREA TA <<<
      - name: Fix MNN - disable DEMO that needs audio/audio.hpp
        run: |
          echo "=== Fixing MNN CMake ==="
          # 1. Fix in llm/mnn submodule if exists
          if [ -f "llm/mnn/CMakeLists.txt" ]; then
            echo "Patching llm/mnn/CMakeLists.txt"
            # Forteaza OFF pentru demo/test/tools
            grep -q "MNN_BUILD_DEMO" llm/mnn/CMakeLists.txt && sed -i 's/.*MNN_BUILD_DEMO.*/set(MNN_BUILD_DEMO OFF CACHE BOOL "" FORCE)/g' llm/mnn/CMakeLists.txt || echo 'set(MNN_BUILD_DEMO OFF CACHE BOOL "" FORCE)' >> llm/mnn/CMakeLists.txt
            echo 'set(MNN_BUILD_TEST OFF CACHE BOOL "" FORCE)' >> llm/mnn/CMakeLists.txt
            echo 'set(MNN_BUILD_TOOLS OFF CACHE BOOL "" FORCE)' >> llm/mnn/CMakeLists.txt
            echo 'set(MNN_BUILD_BENCHMARK OFF CACHE BOOL "" FORCE)' >> llm/mnn/CMakeLists.txt
          fi
          
          # 2. Fix in modulul mnn (wrapper-ul vostru)
          MNN_BUILD_FILE=$(find . -maxdepth 4 -type f -name "build.gradle*" | xargs grep -l "MNN\|mnn" | grep mnn | head -1)
          if [ -z "$MNN_BUILD_FILE" ]; then
            MNN_BUILD_FILE="mnn/build.gradle"
          fi
          if [ -f "$MNN_BUILD_FILE" ]; then
            echo "Found mnn build file: $MNN_BUILD_FILE"
            cat "$MNN_BUILD_FILE"
            # Adauga argumentele necesare daca nu exista
            if ! grep -q "MNN_BUILD_DEMO" "$MNN_BUILD_FILE"; then
              sed -i 's/arguments {/arguments {\n            "-DMNN_BUILD_DEMO=OFF",\n            "-DMNN_BUILD_TEST=OFF",\n            "-DMNN_BUILD_TOOLS=OFF",\n            "-DMNN_BUILD_BENCHMARK=OFF",\n            "-DMNN_SEP_BUILD=OFF",/g' "$MNN_BUILD_FILE" || true
            fi
          fi

          # 3. Fallback: sterge direct fisierul demo care pica
          rm -f llm/mnn/transformers/llm/engine/demo/qwen3_tts_demo.cpp || true
          echo "Fix done"

      - name: Setup DragonBones Prebuilt AAR
        run: |
          mkdir -p app/libs
          if [ ! -f app/libs/dragonbones-release.aar ]; then
            curl -L --retry 3 -o app/libs/dragonbones-release.aar https://github.com/Davinci198/dragonbones-aar-prebuilt/releases/download/v1.0.0-ndk27-fix/dragonbones-release.aar
          fi
          ls -lh app/libs/dragonbones-release.aar

      - name: Install JS deps
        run: |
          npm install -g pnpm@10.34.5
          npm ci --no-audit --no-fund
          npm --prefix web-chat ci --no-audit --no-fund

      - name: Build WebChat + ToolPkg
        run: |
          npm --prefix web-chat run typecheck || true
          npm run build:webchat || true
          npm run build:examples:github || true
          python3 tools/example_packages/sync_example_packages.py --no-hot-reload || true

      - name: Configure SDK
        run: echo "sdk.dir=$ANDROID_HOME" > local.properties

      - name: Run Gradle build - CLONE
        env:
          TASK: ${{ inputs.gradle_task }}
        run: |
          set -euo pipefail
          if [ -z "${TASK:-}" ]; then TASK=":app:assembleCloneDebug"; fi
          echo "Building $TASK as com.ai.assistance.operit.clone"
          chmod +x ./gradlew
          ./gradlew $TASK --stacktrace --no-build-cache --no-daemon --max-workers=2 -Dorg.gradle.jvmargs="-Xmx7g -Xms2g -XX:MaxMetaspaceSize=1g -XX:+UseG1GC" | tee gradle.log

      - name: Upload APK Clone
        if: success()
        uses: actions/upload-artifact@v6
        with:
          name: fix-operit-clone-${{ github.run_number }}
          path: |
            app/build/outputs/apk/clone/**/*.apk
            app/build/outputs/apk/**/*.apk
          if-no-files-found: error
          retention-days: 14

      - name: Upload log on fail
        if: failure()
        uses: actions/upload-artifact@v6
        with:
          name: gradle-log-${{ github.run_number }}
          path: |
            gradle.log
            app/.cxx/**/CMakeOutput.log
            app/.cxx/**/CMakeError.log
          if-no-files-found: warn
          retention-days: 7
